name: Update ERR archive podcast feed

on:
  push:
    branches: [master]
    paths-ignore:
      - 'feed.xml'
  workflow_dispatch:
  schedule:
    - cron: '17 3 * * 1'

permissions:
  contents: write

jobs:
  build-feed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install tools
        run: |
          python -m pip install --upgrade pip yt-dlp playwright
          python -m pip install 'https://github.com/smarbaa/yt-dlp-ugly-err/archive/master.zip'
          python -m playwright install --with-deps chromium
      - name: Generate feed
        run: python generate_feed.py
      - name: Commit feed
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add feed.xml
          git diff --cached --quiet || git commit -m 'Update Rahva oma kaitse 2005-2009 feed'
          git push
